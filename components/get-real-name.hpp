#ifndef GET_REAL_NAME_HPP
#define GET_REAL_NAME_HPP

#pragma once

#include <string>
#include <vector>

// inline std::string getRealName(std::vector<std::string> params)
// {
	// std::string real_name;
	// int i = 4;
	// if (params.size() == 5 && !params[4].compare(":"))
	// 	throw (461);// need more params
	// if (params.size() > 4 && params[4].at(0) == ':')
	// {
	// 	real_name = params[4].substr(1) + " "; // start after (:) to last + space
	// 	i++;
	// }
	// while (i < params.size())
	// {
	// 	real_name += params[i] + " ";
	// 	i++;
	// }
	// return (real_name);
// }

#endif